# Alias for exit
# Didn't wanna do just 'x' to avoid accidents
# This file isn't actually used cause of technical reasons
# The command's implementation is in setup/generate-wrappers and setup/populate-profile

exit
